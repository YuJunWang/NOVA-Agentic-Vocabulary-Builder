name: NOVA Daily Data Collection

on:
  schedule:
    # 每天台灣時間早上 8:00 執行 (UTC 0:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # 允許我們在 GitHub 網頁上手動點擊執行
    inputs:
      target_daily_count:
        description: '本次手動執行目標單字數量 (預設 5)'
        required: false
        default: '5'

jobs:
  collect-vocab:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v6

      - name: 🐍 Set up Python 3.10
        uses: actions/setup-python@v7
        with:
          python-version: '3.10'
          cache: 'pip' # 加入快取機制，讓未來的安裝速度更快

      - name: 📦 Cache HuggingFace Models
        uses: actions/cache@v5
        with:
          path: ~/.cache/huggingface
          key: ${{ runner.os }}-huggingface-all-MiniLM-L6-v2
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🚀 Run NOVA Collector Engine
        env:
          # 從 GitHub Secrets 讀取機密並變成環境變數
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_MODEL: ${{ vars.GROQ_MODEL || 'openai/gpt-oss-120b' }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          TARGET_DAILY_COUNT: ${{ inputs.target_daily_count || vars.TARGET_DAILY_COUNT || 5 }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python collector.py